pipeline {
    agent any
    stages {
   
      stage('Build') {
        steps {
          git branch: 'main', url: 'https://github.com/NatalliaSem/Tests'
        }
      }
      stage('Test runs') {
        steps {
          echo 'Testing using tests'
          sh 'python3 tests.py'
        }
      }
      stage('Deploys') {
        steps {
         sh 'git checkout pre-prod'
         sh 'git merge main'
         sh 'git branch'
         sh 'git add .'
         sh 'git commit -m "comment"'
         sh 'git remote set-url origin https://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@github.com/NatalliaSem/Tests.git'
         sh 'git push https://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@github.com/NatalliaSem/Tests.git'         
        }
      }
      stage('Monitor') {
     steps {
       echo 'Well done!'
     }
   }
    }    
  }
