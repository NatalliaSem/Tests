pipeline {
    agent any
    stages {
   
      stage('Build') {
        steps {
          git branch: 'main', url: 'https://github.com/NatalliaSem/Tests'
        }
      }
      stage('Test runs') {
        steps {
          echo 'Testing using tests'
          sh 'python3 tests.py'
        }
      }
      stage('Deploy') {
        steps {
         sh 'git checkout pre-prod'
         sh 'git merge main'
         sh 'git branch'
         sh 'git add .'
         sh 'git commit -m "comment"'
         sh 'git push https://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@github.com/NatalliaSem/Tests.git'         
        }
      }
      stage('Monitor') {
     steps {
       echo 'Well done!'
     }
   }
    }    
  }
